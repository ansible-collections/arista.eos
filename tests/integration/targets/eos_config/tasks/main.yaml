---
- include: cli.yaml
  tags:
    - network_cli

- include: cli_config.yaml
  tags:
    - network_cli

- include: eapi.yaml
  tags:
    - httpapi

- include: redirection.yaml
  when: ansible_version.full is version('2.10.0', '>=')
