---
merged:
  commands:
    - hostname eos_test

replaced:
  before:
    hostname: "eos_test"
  after:
    hostname: "eos"
  commands:
    - hostname eos

deleted:
  before:
    hostname: "eos_test"
  commands:
    - no hostname eos_test
