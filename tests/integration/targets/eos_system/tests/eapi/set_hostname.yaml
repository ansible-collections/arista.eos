---
- ansible.builtin.debug: msg="START eapi/set_hostname.yaml"

- name: setup
  become: true
  arista.eos.eos_config:
    lines: hostname switch
    match: none

- name: configure hostname
  become: true
  register: result
  arista.eos.eos_system:
    hostname: foo

- ansible.builtin.assert:
    that:
      - result.changed == true

- name: verify hostname
  become: true
  register: result
  arista.eos.eos_system:
    hostname: foo

- ansible.builtin.assert:
    that:
      - result.changed == false

<<<<<<< HEAD
- ansible.builtin.debug:
    msg: " debug_komal {{ inventory_hostname_short }}"

=======
>>>>>>> 041a483 (fixed eos downstream test?)
- name: teardown
  become: true
  arista.eos.eos_config:
    lines: hostname switch
    match: none

- ansible.builtin.debug: msg="END eapi/set_hostname.yaml"
